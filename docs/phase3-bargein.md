# Phase 3 — True Barge-in (WIP)

Tracking items for the upcoming barge-in work:

- [x] Frontend: ff=barge_in toggle, suspend/resume hooks, tail pad + duck telemetry.
- [x] Backend: handle response.suspend_audio/response.resume_audio, expose Prometheus counters.
- [x] Tests: add Playwright duck/resume scenario and backend integration probes.

This placeholder keeps the branch alive while the implementation is in flight.
