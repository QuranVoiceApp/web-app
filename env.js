// Copy to env.js and adjust WS_URL as needed for your deployment
window.Env = {
  // Backend Realtime WS proxy (ensure CORS/origin allowed on server)
  WS_URL: 'wss://quran.asimo.io/realtime/v1/ws'
};

